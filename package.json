{
  "name": "dew",
  "version": "0.0.1",
  "description": "Minimalist Desktop HUD & Luau Applet Platform",
  "private": true,
  "scripts": {
    "dev": "cargo run --manifest-path host/Cargo.toml --",
    "build": "cargo build --release --manifest-path host/Cargo.toml",
    "timetracker": "cargo run --manifest-path host/Cargo.toml -- --applet timetracker",
    "calculator": "cargo run --manifest-path host/Cargo.toml -- --applet calculator",
    "nameplate": "cargo run --manifest-path host/Cargo.toml -- --applet nameplate",
    "snapshot": "cargo run --manifest-path host/Cargo.toml -- --snapshot dew.png"
  }
}
